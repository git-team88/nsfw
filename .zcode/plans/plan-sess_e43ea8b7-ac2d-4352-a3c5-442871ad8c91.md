1. 在 `src/api/index.ts` 增加 `getContentSwitchPublic()`，调用 `index/getContentSwitchPublic`，并为所有已有 `show_nsfw` 的公开内容 API 增加可选的内容开关上下文参数：当服务端模式为 2 时发送 `channel=2`；同时修正 truthy 判断，确保 `show_nsfw=0` 也会真正发出。搜索、相似内容、作者页等 POST 请求统一由调用方传入完整字段。

2. 新增一个轻量的共享内容策略模块（优先沿用 Pinia/现有前端结构，不重复创建各页面状态），负责：
   - 首次使用时请求 `getContentSwitchPublic` 并缓存结果，兼容接口返回值位于 `data`、`data.content_switch`/`data.show_nsfw` 等常见字段，以及直接返回数字；无法识别时保守回退为 1，保持当前行为。
   - 将三种模式映射为统一策略：0 固定 `show_nsfw=0` 且隐藏开关；1 使用用户当前开关且显示开关；2 固定 `show_nsfw=1`、隐藏开关，并在相关内容请求中附加 `channel=2`。
   - 初始化用户开关时优先同步已有 `userInfo.info.show_nsfw`，匿名用户继续使用 `localStorage.allowSensitiveContent`，并在模式 0/2 时覆盖本地值，避免页面之间出现不同结果。

3. 改造首页 `Home.vue` 与 `HomeShowcase.vue`：页面加载内容前先 `await` 内容策略初始化；根据策略控制敏感内容开关的两处显示、切换处理和首页推荐/关注/订阅/排行榜参数；模式 0 禁止切换，模式 2 不显示并固定 1，模式 1 保持现有用户控制；避免在策略尚未返回时误发请求。

4. 改造所有公开内容读取调用方，使它们在请求前确保策略已初始化，并统一使用策略生成的 `show_nsfw`/`channel`：`Detail.vue`、`NovelDetail.vue`、`CollectionDetail.vue`、`Search.vue`、`Similar.vue`、`NovelGenerate.vue`、`Ranking.vue`、`UserHome.vue` 以及 `Sidebar.vue` 的详情/评论刷新请求。移除榜单作品固定传 1 和各处直接读取 localStorage 的分歧；详情手工 fetch 的 body 也带上模式 2 所需的 `channel=2`。

5. 调整个人中心/编辑页的开关展示和保存逻辑：模式 0/2 隐藏该开关并显示对应固定状态；模式 1 保留后端 `modifyShowNsfw` 的用户控制。保留登录用户后端状态同步，但不让页面初始化把服务端强制模式覆盖掉。

6. 逐个复核 `rg 'show_nsfw|allowSensitiveContent|channel'`，确认没有漏掉公开内容请求、没有在模式 0 漏传 1、没有在模式 2 漏传 `channel=2`；运行 `npm run type-check`、`npx eslint .`、`npm run build`。若环境允许，再启动开发服务器做三种返回值的浏览器冒烟验证，并记录实际响应结构后收紧解析逻辑。