# 项目协作规则

## 任务处理

- 用户每次提交的任务，需要合理拆分到多个子智能体（sub-agents）并行处理，以提高处理速度。
- 拆分仅在任务确实可并行、子任务相互独立时进行；对于简单的单点改动直接处理，避免过度拆分反而拖慢。

## 代码修改（重要）

- 每次修改前，必须先读取目标文件的**最新内容**再动手，以磁盘当前内容为准，不要凭记忆或旧版本改，避免覆盖掉其他任务/其他人已经改过的功能。
- 涉及多文件、或不确定文件是否被改过时，先查 `git status` / `git log -1` / 对应文件的最新 diff，确认基线后再改。
- 用唯一上下文精确定位、只改目标片段，不做整段覆盖，减少误伤已有代码。
- 改完做校验（类型检查 + 关键点复核），确认没有把已有功能改坏或改没。
